package foundation.polar.gratify.buffer;

import foundation.polar.gratify.utils.logging.LoggerFactory;
import org.apache.commons.logging.Log;

/**
 * A collection of utility methods that is related with handling {@link ByteBuffer},
 * such as the generation of hex dump and swapping an integer's byte order.
 */
public final class ByteBufferUtils {
   private static final Log logger = LoggerFactory.getLog(ByteBufferUtils.class);
}
